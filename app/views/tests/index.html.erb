<h1><%= t('tests.index.header') %></h1>
<% if @tests.present?%>
<table>
  <thead>
    <tr>
      <td>Заголовок теста</td>
      <td>Уровень</td>
      <td>Количество вопросов</td>
      <td>Действия</td>
    </tr>
  </thead>
  <tbody>
    <%= render @tests%>
  </tbody>
</table>
<% else %>
 <h1>Нет тестов...</h1>
<% end %>