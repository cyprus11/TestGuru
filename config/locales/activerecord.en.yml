en:
  activerecord:
    models:
      test: 'Test'
    attributes:
      test:
        title: 'Title'
    errors:
      messages:
        record_invalid: "some errors: %{errors}"
        blank: "can't be blank"